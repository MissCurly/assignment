#include <stdio.h>
#include<math.h>
int main()
{
   int x,y,z;
   printf("enter the value of x,y:");
   scanf("%d %d", &x, &y);
   
   z=pow(x, 2*y*y) -4*x;
   printf("\nvalue of mean is:%d", z );
   return 0; 
}
